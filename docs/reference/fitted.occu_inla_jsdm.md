# Extract fitted values from a JSDM

Extract fitted values from a JSDM

## Usage

``` r
# S3 method for class 'occu_inla_jsdm'
fitted(object, ...)
```

## Arguments

- object:

  fitted occu_inla_jsdm object

- ...:

  ignored

## Value

named list of per-species fitted probability vectors

# Extract fitted values from a multi-species occupancy model

Extract fitted values from a multi-species occupancy model

## Usage

``` r
# S3 method for class 'occu_inla_ms'
fitted(object, ...)
```

## Arguments

- object:

  fitted occu_inla_ms object

- ...:

  ignored

## Value

named list of per-species fitted value lists

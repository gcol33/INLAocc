# Print method for occu_spatial

Print method for occu_spatial

## Usage

``` r
# S3 method for class 'occu_spatial'
print(x, ...)
```

## Arguments

- x:

  an `occu_spatial` object

- ...:

  additional arguments (ignored)

## Value

The `occu_spatial` object `x`, returned invisibly.

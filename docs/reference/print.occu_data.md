# Print method for occu_data

Print method for occu_data

## Usage

``` r
# S3 method for class 'occu_data'
print(x, ...)
```

## Arguments

- x:

  an `occu_data` object

- ...:

  additional arguments (ignored)

## Value

The `occu_data` object `x`, returned invisibly.

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu.R, R/occu_fit.R
\name{occu}
\alias{occu}
\alias{occu_inla}
\alias{spatial_occu_inla}
\alias{intOccu_inla}
\alias{spIntOccu_inla}
\alias{ms_occu_inla}
\alias{lfMsOccu_inla}
\alias{sfMsOccu_inla}
\alias{svcOccu_inla}
\alias{temporal_occu_inla}
\alias{lfJSDM_inla}
\title{Fit occupancy models using INLA}
\usage{
occu(
  occ.formula,
  det.formula = NULL,
  data,
  spatial = NULL,
  temporal = NULL,
  multispecies = FALSE,
  integrated = FALSE,
  n.factors = NULL,
  svc = NULL,
  spde.args = list(),
  priors = NULL,
  occ.re = NULL,
  det.re = NULL,
  max.iter = 50L,
  tol = 1e-04,
  damping = 0.3,
  ensemble = FALSE,
  num.threads = "1:1",
  k.fold = 0L,
  verbose = 1L
)
}
\arguments{
\item{occ.formula}{RHS formula for occupancy (psi). Supports mixed-model
random effects syntax: \code{~ elev + forest + (1 | region)}.}

\item{det.formula}{RHS formula for detection (p). NULL for JSDM models
(set \code{multispecies = "jsdm"}).}

\item{data}{An \code{occu_data} or \code{occu_data_ms} object, or a raw
list with components \code{y}, \code{occ.covs}, \code{det.covs}
(spOccupancy-compatible).}

\item{spatial}{N x 2 coordinate matrix or \code{occu_spatial()} object.
Enables spatial SPDE models.}

\item{temporal}{\code{"ar1"} or \code{"iid"} for multi-season models.
NULL (default) for single-season.}

\item{multispecies}{\code{FALSE} (default), \code{TRUE} for community
models, or \code{"jsdm"} for joint species distribution models
(no detection process).}

\item{integrated}{\code{TRUE} for multi-data-source models. Requires
\code{data$y} to be a list of detection matrices.}

\item{n.factors}{Integer number of latent factors. Enables latent factor
variants of multi-species models.}

\item{svc}{Integer vector of occupancy design matrix columns that get
spatially-varying coefficients (1 = intercept). Requires \code{spatial}.}

\item{spde.args}{List of arguments passed to \code{occu_spatial()} when
\code{spatial} is a coordinate matrix.}

\item{priors}{\code{occu_priors()} object or spOccupancy-compatible named
list.}

\item{occ.re}{Explicit list of \code{occu_re()} specs for occupancy.}

\item{det.re}{Explicit list of \code{occu_re()} specs for detection.}

\item{max.iter}{Maximum EM iterations (default 50).}

\item{tol}{Convergence tolerance (default 1e-4).}

\item{damping}{EM damping factor 0--1 (default 0.3).}

\item{ensemble}{Logical; if \code{TRUE}, use ensemble averaging across
multiple imputation chains (default \code{FALSE}).}

\item{num.threads}{Thread specification for INLA, as \code{"A:B"} where A is
outer and B is inner OpenMP threads (default \code{"1:1"}).}

\item{k.fold}{Number of cross-validation folds (default 0, no CV).}

\item{verbose}{0 = silent, 1 = iteration summaries, 2 = full INLA output.}
}
\value{
An S3 object whose class depends on the model type (e.g.,
\code{"occu_inla"}, \code{"occu_inla_spatial"}, \code{"occu_inla_ms"}).
}
\description{
Single entry point for all occupancy model types. The model structure is
controlled by flags: \code{spatial}, \code{temporal}, \code{multispecies},
\code{integrated}, \code{n.factors}, and \code{svc}. Covers all model types
from \pkg{spOccupancy} (PGOcc, spPGOcc, tPGOcc, msPGOcc, etc.) via
flag combinations.
}
\examples{
\donttest{
# Single-species (cf. PGOcc)
# fit <- occu(~ elev, ~ effort, data)

# Spatial (cf. spPGOcc)
# fit <- occu(~ elev, ~ effort, data, spatial = coords)

# Multi-species (cf. msPGOcc)
# fit <- occu(~ elev, ~ effort, ms_data, multispecies = TRUE)

# Temporal (cf. tPGOcc)
# fit <- occu(~ elev, ~ effort, data, temporal = "ar1")
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_data.R
\name{occu_data}
\alias{occu_data}
\title{Convert a data.frame to occupancy data}
\usage{
occu_data(df, y, site, visit, occ.covs = NULL, det.covs = NULL, coords = NULL)
}
\arguments{
\item{df}{a data.frame in long format (one row per site-visit).}

\item{y}{character: name of the detection column (0/1/NA).}

\item{site}{character: name of the site identifier column.}

\item{visit}{character: name of the visit/replicate column (integer
or factor indicating visit number within each site).}

\item{occ.covs}{character vector of column names for site-level
occupancy covariates. If \code{NULL} (default), all columns that
are constant within each site (excluding \code{y}, \code{site},
\code{visit}, and \code{det.covs}) are used.}

\item{det.covs}{character vector of column names for visit-level
detection covariates. If \code{NULL} (default), all columns that
vary within at least one site are used.}

\item{coords}{character vector of length 2 giving coordinate column
names, or \code{NULL}.}
}
\value{
An object of class \code{"occu_data"} ready for
\code{\link{occu}}.
}
\description{
Converts a long-format data.frame (one row per site-visit) into the
structured list format required by \code{\link{occu}}. Pipe-friendly:
the data.frame is the first argument.
}
\examples{
# Long-format data
df <- data.frame(
  site = rep(1:50, each = 4),
  visit = rep(1:4, 50),
  detected = rbinom(200, 1, 0.4),
  elev = rep(rnorm(50), each = 4),
  effort = runif(200, 1, 8)
)
dat <- occu_data(df, y = "detected", site = "site", visit = "visit")

# With pipe
# dat <- df |> occu_data(y = "detected", site = "site", visit = "visit")

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_spatial.R
\name{occu_areal}
\alias{occu_areal}
\title{Create an areal spatial effect for occupancy models (CAR/BYM2)}
\usage{
occu_areal(
  adj,
  model = c("bym2", "besag"),
  prior.sigma = c(1, 0.05),
  scale.model = TRUE
)
}
\arguments{
\item{adj}{Adjacency structure: a symmetric matrix, a \code{nb} object
(from \code{spdep}), or a path to an INLA graph file.}

\item{model}{\code{"bym2"} (default, recommended) or \code{"besag"}.
BYM2 separates structured (spatial) and unstructured (iid) components
with a mixing parameter.}

\item{prior.sigma}{numeric(2): PC prior on marginal SD. c(s0, p) means
P(sigma > s0) = p. Default c(1, 0.05).}

\item{scale.model}{Logical: scale the precision matrix so the generalized
variance is 1 (recommended for bym2). Default TRUE.}
}
\value{
object of class \code{"occu_areal"} with graph and model components
}
\description{
For data on grids, administrative units, or any areal structure. Uses
INLA's \code{besag} or \code{bym2} model with an adjacency graph.
}

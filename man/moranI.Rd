% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_diagnostics.R
\name{moranI}
\alias{moranI}
\title{Moran's I test for spatial autocorrelation}
\usage{
moranI(
  object,
  coords = NULL,
  weights = c("inverse", "knn"),
  k = 10L,
  resid.type = "deviance",
  alternative = c("two.sided", "greater", "less")
)
}
\arguments{
\item{object}{fitted \code{occu_inla} object, or a numeric vector of
residuals (in which case \code{coords} must be supplied)}

\item{coords}{optional N x 2 coordinate matrix.
If \code{object} is an \code{occu_inla} fit, defaults to
\code{object$data$coords}.}

\item{weights}{weight scheme: \code{"inverse"} (default) uses
inverse-distance weights on all pairs (matches DHARMa),
\code{"knn"} uses k nearest-neighbour binary weights}

\item{k}{number of nearest neighbours (only used when
\code{weights = "knn"}, default 10)}

\item{resid.type}{residual type passed to \code{residuals()} if
\code{object} is a fit (default \code{"deviance"})}

\item{alternative}{\code{"two.sided"} (default), \code{"greater"}, or
\code{"less"}}
}
\value{
A list of class \code{"htest"} with components \code{statistic},
\code{p.value}, \code{parameter} (expected I), and \code{method}.
}
\description{
Computes Moran's I on occupancy residuals using an inverse-distance
weight matrix (k nearest neighbours).  No external dependencies —
uses the normal approximation under the randomisation assumption.
}

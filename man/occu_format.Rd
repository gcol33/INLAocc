% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_data.R
\name{occu_format}
\alias{occu_format}
\title{Format data for INLA occupancy models}
\usage{
occu_format(y, occ.covs = NULL, det.covs = NULL, coords = NULL, species = NULL)
}
\arguments{
\item{y}{N x J detection history matrix (0/1/NA). Rows = sites, cols = visits.}

\item{occ.covs}{data.frame with N rows of site-level occupancy covariates.
Can also be a named list of vectors, each length N.}

\item{det.covs}{named list of detection covariates. Each element is either:
\itemize{
\item a vector of length N (constant across visits)
\item an N x J matrix (varies by visit)
}}

\item{coords}{optional N x 2 matrix of site coordinates (for spatial models)}

\item{species}{optional character or integer species identifier (for multi-species)}
}
\value{
An object of class \code{"occu_data"} with validated components
}
\description{
Accepts data in spOccupancy-compatible format and validates it.
}
\examples{
y <- matrix(rbinom(200, 1, 0.4), nrow = 50, ncol = 4)
occ_covs <- data.frame(elev = rnorm(50), forest = runif(50))
det_covs <- list(
  effort = matrix(runif(200, 1, 8), 50, 4),
  date   = matrix(rnorm(200), 50, 4)
)
dat <- occu_format(y, occ_covs, det_covs)
}

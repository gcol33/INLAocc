% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occu_diagnostics.R
\name{variogram}
\alias{variogram}
\title{Empirical semivariogram of occupancy residuals}
\usage{
variogram(
  object,
  coords = NULL,
  n.bins = 15L,
  max.dist = NULL,
  resid.type = "deviance"
)
}
\arguments{
\item{object}{fitted \code{occu_inla} object, or a numeric vector of
residuals (in which case \code{coords} must be supplied)}

\item{coords}{optional N x 2 coordinate matrix (defaults to
\code{object$data$coords})}

\item{n.bins}{number of distance bins (default 15)}

\item{max.dist}{maximum distance to consider (default: half the max
pairwise distance)}

\item{resid.type}{residual type (default \code{"deviance"})}
}
\value{
A data.frame of class \code{"occu_variogram"} with columns
\code{dist} (bin midpoint), \code{gamma} (semivariance), and
\code{n.pairs} (number of pairs in bin).  Has a \code{plot()} method.
}
\description{
Computes the empirical semivariogram in distance bins.  Useful for
visually assessing whether spatial structure remains in the residuals
after model fitting.
}
